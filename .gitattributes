# Normalize line endings to LF across platforms (important on Windows/WSL2).
* text=auto eol=lf

# Treat these as binary so they are never line-ending normalized.
*.bin binary
*.png binary
*.ico binary
